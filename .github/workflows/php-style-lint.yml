name: "PHP Style Linting"

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  build_app:
    name: "PHPCS"
    runs-on: ubuntu-latest

    steps:
      - name: Skip
        run: echo 'workflow broke - skip for PR checks'

  validate-readme-spacing:
    name: Validate README Spacing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - uses: pantheon-systems/validate-readme-spacing@13c5e619ef5fc39632292f1f5261ab31d1904208 # v1
